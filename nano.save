#!/bin/bash

echo "🔐 Setting up ssh-agent for GitHub..."

# Start the ssh-agent
eval "$(ssh-agent -s)"

# Add the private key to the agent
ssh-add ~/.ssh/id_rsa

# Verify key added
ssh-add -l

echo "✅ ssh-agent ready"
